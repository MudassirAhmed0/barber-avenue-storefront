import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    domains: ["fadedculture.com", "randomuser.me", "tuk-cdn.s3.amazonaws.com", "upload.wikimedia.org", "cdn.shopify.com"],
  },
};

export default nextConfig;
